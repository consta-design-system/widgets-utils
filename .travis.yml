language: node_js
node_js: '10'

cache: yarn

before_install:
  - npm install -g yarn npm-cli-login
  - npm-cli-login -u ${LOGIN} -p ${PASSWORD} -e tools@csssr.com -r https://nexus.csssr.space/repository/csssr -s @gaz
  - yarn --frozen-lockfile
